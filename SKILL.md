---
name: reading-roadmap
description: 读书/教程阅读路线规划。给定一本在线书或教程，先询问阅读目的（兴趣/原理/求职/解决问题/系统掌握）、领域熟悉度、每日可投入时间，再产出按目标重排的章节阅读顺序、逐日学习计划与可打卡的 HTML 导读卡。触发场景：用户说"给个阅读顺序""不想从第一章开始读""按我的目标安排一下""具体到每一天""阅读路线""导读卡"等。当前版本仅支持有在线章节的文章/教程/文档；本地电子书、纸质书不支持（无在线版时如实告知并降级为文字路线）。不做读后摘要、书评、内容提炼。
---

# Reading Roadmap · 阅读路线规划

给定一本书或在线教程，产出"可直接照做"的章节阅读顺序 + 逐日计划 + 可打卡导读卡。核心是**先问清目的再定序**，不是把目录复述一遍。

## 范围（v1）

- 支持：有可访问在线章节的文章 / 教程 / 文档 / 开源仓库（docsify、GitBook、GitHub 等）。
- 不支持：本地电子书（EPUB/PDF）、纸质书。目标无在线版时如实告知，降级为对话内文字路线，不假装支持。

## 硬规则

1. **先问后答**：未拿到三问答案前，不得直接给路线（见「三问画像」）。
2. **链接真实**：在线链接必须来自真实抓取或源码推算，并逐条验证；禁止凭印象拼 URL。
3. **顺序是建议**：路线是基于目的 × 依赖的重构判断，交付时说明依据，不冒充官方读法。
4. **有节奏**：逐日计划必须有「读 + 写/测 + 复盘」结构，不允许只堆章节名。
5. **交付可校验**：链接可达、结构、JS 语法逐项核对后才能交付（见「交付与校验」）。

## 工作流

```
1. 三问画像       → 目的（多选排序）/ 领域熟悉度 / 每日时间（见 references/profiling.md）
2. 摸结构         → 抓取真实目录/章节地图（README、仓库树、站点导航），标出章节依赖
3. 判断站点技术栈  → 决定链接形态（见 references/link-authoring.md）
4. 按目的定序     → 类型 × 目的 → 路线策略（见 references/routing.md）
5. 逐日化         → 拆到每天：读 + 写/测；每周固定复盘日（见 references/pacing.md）
6. 生成链接       → 章节链接，能精确到小节锚点就精确（?id=）
7. 交付           → 默认独立 HTML 导读卡；轻量场景可对话内表格（见 references/deliverables.md）
8. 三层校验       → 链接可达 / HTML 结构 / JS 语法（截图自检失败可跳过）
```

## 三问画像（强制，先问后答）

一次给出选项让用户选，不逐个追问，也不先猜：

- **阅读目的**（多选，标注排序）：
  A 兴趣 / B 原理与技术点 / C 求职面试或考试 / D 解决具体问题 / E 系统掌握
- **领域熟悉度**（按书类型换问法，见 profiling.md）：
  如技术教程问编程基础；方法论书问是否读过同主题；不加猜测。
- **每日可投入**：5 分钟 / 15 分钟 / 30 分钟 / 周末集中。
- **其他备注**（可选，开放输入）：时间约束 / 内容偏好 / 前置情况等，用户没写就跳过，写了必须纳入定序考量。

用户已主动给出时跳过提问，但必须在回复里复述确认。

## 类型决策表（v1：C、B 已启用，A、D 暂缓）

| 类型 | 判断特征 | 路线策略 |
| --- | --- | --- |
| C 教程/体系文档 | 章节有前置依赖、循序渐进 | 按依赖序重排 + 练习穿插；概念→范式→动手→纵深 |
| B 在线方法论/长文 | 章节相对独立、按主题组织 | 按目的直读相关部分，先读与目标直接相关的章节 |
| A 叙事类 | 线性叙事（小说/传记） | 暂缓：定序价值低，顺序读或兴趣切口 |
| D 案例集/手册 | 无强顺序、按条目组织 | 暂缓：问题驱动跳读 |

决策细节与目的 × 类型矩阵见 references/routing.md。

## 逐日化

- 每日 = 读 X 分钟 + 写/测 Y 分钟（默认 10 + 5，按时间预算缩放）。
- 每周固定一个复盘日（不补新课，回看本周链接自测）。
- 打卡是纯工具：勾选状态由用户自行管理，不做中断/续读/补卡约束，不写纪律文案。
- 导读卡不堆规则说明：必要信息为主，规则文案最多一句，能省则省。

## 交付与校验

- 默认交付：独立 HTML 导读卡（用 assets/guide-template.html，替换占位符即成）。
- 轻量场景：对话内 renderer 表格（先结论后表格，遵守 doubao-visualization 协议）。
- 校验清单：占位符无残留 / 勾选框数=天数 / 在线链接逐条验证 / 无 emoji / 结构 + JS 语法校验。
- 降级链：独立 HTML → 对话表格 → 结构化文字；任何层级都不得编造 URL 或留空链接列。

## 资源

- `references/profiling.md`：三问问题集与选项定义
- `references/routing.md`：类型 × 目的决策矩阵与路线模板
- `references/rationale.md`：定序依据（学习科学）
- `references/pacing.md`：逐日化规则
- `references/link-authoring.md`：站点技术栈识别、docsify 锚点规则、链接验证与回退
- `references/deliverables.md`：导读卡生成契约与校验清单
- `assets/guide-template.html`：可复用导读卡模板（占位符式）
